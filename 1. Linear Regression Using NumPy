import numpy as np

x = np.array([1, 2, 3, 4, 5])
y = np.array([2, 4, 6, 8, 10])

x_mean = np.mean(x)
y_mean = np.mean(y)

m = np.sum((x - x_mean)*(y - y_mean)) / np.sum((x - x_mean)**2)
c = y_mean - m * x_mean

print("Slope:", m)
print("Intercept:", c)
